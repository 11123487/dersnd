#include <stdio.h>
#include <stdlib.h>
#include <string.h>
int S(int x)
{
    int i,sum=0;
    for(i=1;i<=x;i++)
        sum+=i;
    return sum;
}
int main()
{
    double s;
    int i,x;
    scanf("%d",&x);
    for(i=1;i<=x;i++)
        s+=1.0/S(i);
    printf("s=%lfd",s);
}
