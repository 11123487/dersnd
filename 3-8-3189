#include <stdio.h>
#include <stdlib.h>
#include <string.h>
int S(int x)
{
    int sum=0;
    while(x)
    {
        sum++;
        x/=10;
    }
    return sum;
}
int main()
{
    int a;
    scanf("%d",&a);
    if(a==0) 
    {
        printf("1");
    }
    else
    printf("%d",S(a));
}
